import React from 'react';

const HubView = () => {
  return (
    <div className="hub-view">
      <h4>Hub View</h4>
      {/* Add a static image here representing the whole slide */}
      <img src="https://via.placeholder.com/150" alt="Hub View" />
    </div>
  );
};

export default HubView;
